用户登录成功:
  url: api/v1/pub/login/3rd
  method: POST
  remark: random_number(10)
  is_run: True
  data:
      {
    "ID": "chenzanxu",
    "secret": "",
    "type": random_number(10)
}
  extract_key:
    access_token: $.data.access_token
  assert_response_value:
  -
    expr: $.status
    expected: 1
    assert_type: equals_assert

用户登录成功1:
  url: api/v1/pub/login/3rd
  method: POST
  remark: random_number(10)
  is_run: True
  data:
      {
    "ID": "chenzanxu",
    "secret": "",
    "type": random_number(10)
}
  extract_key:
    access_token: $.data.access_token
  assert_response_value:
  -
    expr: $.status
    expected: 1
    assert_type: equals_assert
